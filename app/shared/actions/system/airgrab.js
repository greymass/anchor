import { get, set } from 'dot-prop-immutable';

import * as types from '../types';
import eos from '../helpers/eos';

export function claimAirgrab(data) {
  return (dispatch: () => void, getState) => {
    //   whatever code is needed..
  };
}

export default {
  claimAirgrab
};
